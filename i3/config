# This is a config file for i3 (https://i3wm.org/).
# It was generated by Gemini based on your Hyprland config.
#
# You can find more information on configuring i3 here:
# https://i3wm.org/docs/userguide.html
#
# To use this config, make sure you have i3 installed, then place this file in
# ~/.config/i3/config and log into an i3 session.

# -----------------------------------------------------------------------------
# Variables
# -----------------------------------------------------------------------------

# Font for window titles. Will also be used by the bar unless a different font
# is specified later.
font pango:monospace 8

# The modifier key to be used for most keybindings.
# Your Hyprland config used the Super key (Windows key).
set $mod Mod4

# Your preferred applications from hyprland.conf
set $terminal alacritty
set $fileManager thunar
set $menu rofi -show drun
set $browser chromium

# -----------------------------------------------------------------------------
# Appearance
# -----------------------------------------------------------------------------

# Window borders (new_window)
# Your Hyprland config had a border_size of 1.
new_window pixel 1

# Gaps
# Your Hyprland config had gaps_in = 5 and gaps_out = 10.
gaps inner 5
gaps outer 2

# Colors
# These are translated from your Hyprland active/inactive border colors.
# i3 uses three colors for borders:
#                       border  background text
# client.focused        #33ccff #285577     #ffffff
# client.unfocused      #595959 #222222     #888888
# client.focused_inactive #595959 #222222     #888888

# Active window border color (approximated from your gradient)
set $color_active_border #33ccff
# Inactive window border color
set $color_inactive_border #595959

# Window colors
# class                 border  bground text    indicator child_border
client.focused          $color_active_border $color_active_border #ffffff $color_active_border $color_active_border
client.unfocused        $color_inactive_border #222222 #888888 $color_inactive_border $color_inactive_border
client.focused_inactive $color_inactive_border #222222 #888888 $color_inactive_border $color_inactive_border
client.urgent           #2f343a #900000 #ffffff #900000 #900000

# NOTE on aesthetics:
# Hyprland has a built-in compositor for blur, rounding, and shadows.
# i3 does NOT. To get these effects, you need to install and run a
# separate compositor like 'picom'.
#
# Example: `exec_always --no-startup-id picom --config ~/.config/picom/picom.conf`
# You will need to configure picom to get rounding, shadows, and blur.

# -----------------------------------------------------------------------------
# Keybindings
# -----------------------------------------------------------------------------

# Most of these are translated directly from your hyprland.conf.

# Start a terminal
bindsym $mod+Return exec $terminal

# Start your web browser
bindsym $mod+b exec $browser

# Kill focused window
bindsym $mod+c kill

# Start your application launcher (rofi)
bindsym $mod+d exec $menu

# Start your file manager
bindsym $mod+e exec $fileManager

# Toggle floating mode for a window
bindsym $mod+v floating toggle

# Toggle fullscreen mode for a window
bindsym $mod+f fullscreen toggle

# Change focus (using arrow keys)
bindsym $mod+Left focus left
bindsym $mod+Right focus right
bindsym $mod+Up focus up
bindsym $mod+Down focus down

# Move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Down move down

# Switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# Reload the configuration file
bindsym $mod+Shift+c reload

# Restart i3 inplace (preserves your layout/session)
bindsym $mod+Shift+r restart

# Exit i3 (logs you out of your X session)
bindsym $mod+m exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Screenshot bindings (requires 'scrot' or a similar tool)
# NOTE: hyprshot is for Wayland. A common X11 alternative is 'scrot' or 'flameshot'.
# You will need to install one. The following uses 'flameshot'.
# Region screenshot
bindsym $mod+Print exec flameshot gui
# Fullscreen screenshot
bindsym Print exec flameshot full -p ~/Pictures/
# Window screenshot
bindsym $mod+Shift+Print exec flameshot gui --last-region

# Multimedia keys (these should work out-of-the-box if you have wpctl)
bindsym XF86AudioRaiseVolume exec --no-startup-id wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindsym XF86AudioLowerVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym XF86AudioMute exec --no-startup-id wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindsym XF86AudioMicMute exec --no-startup-id wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set 5%+
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# Player controls (requires playerctl)
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause

# Resize mode
mode "resize" {
        # These bindings trigger as long as you are in resize mode
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt

        # Back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# -----------------------------------------------------------------------------
# Startup Programs
# -----------------------------------------------------------------------------

# exec commands are executed on startup.
# use `exec_always` to run on every i3 restart, not just the first login.
# use `--no-startup-id` so it doesn't show a loading cursor.

# Your Hyprland config started 'waybar' and 'hyprpaper'.
# 'waybar' can work with i3, but 'i3bar' is the native bar.
# 'hyprpaper' is for Wayland. A popular X11 alternative is 'feh'.
#
# Example: exec --no-startup-id feh --bg-scale /path/to/your/wallpaper.png
exec --no-startup-id feh --bg-scale ~/Pictures/1391865.png

# Start a compositor for transparency, shadows, etc.
exec --no-startup-id picom 
exec_always --no-startup-id ~/.config/polybar/launch.sh

# Enable tap-to-touch for touchpad
exec --no-startup-id xinput set-prop "ETPS/2 Elantech Touchpad" "libinput Tapping Enabled" 1

bindsym $mod+w exec --no-startup-id ~/.config/polybar/toggle.sh


